node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Database exports — contain real user data, never committed
/backups

# Brand source assets (logo manual PDF, source SVGs/PNGs) — kept locally,
# only the variants actually used live in static/
/docstest

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
